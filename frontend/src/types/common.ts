type ImageType = {
  src: string;
  alt: string;
  className?: string;
  width?: number;
  height?: number;
};

type Avatar = {
  name: string;
  avatarIcon: string;
  id: string;
};

type AppointmentDetails = {
  type: string;
  status: string;
  iconUrl: string;
};
