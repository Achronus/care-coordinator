export enum FormFieldType {
  INPUT = "input",
  TEXTAREA = "textarea",
  PHONE_INPUT = "phoneInput",
  CHECKBOX = "checkbox",
  DATE_PICKER = "datePicker",
  SELECT = "select",
  SKELETON = "skeleton",
}

export enum Status {
  PENDING = "pending",
  SCHEDULED = "scheduled",
  CANCELLED = "cancelled",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}
